This script allows AutoCAD 'non table' type tables to be exported using the 'Extract Data' functionality and
imported into Excel

Extract data function uses the MTEXT Content and the X and Y geometry to create an excel spreadsheet.

Still many bugs but it works well enough to get stuff done.